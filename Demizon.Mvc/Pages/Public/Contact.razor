@page "/Contact"

<PageTitle>@PageService.GetTitle()</PageTitle>

<MudList T="string">
    <MudListItem T="string" Expanded="true" Text="Jana Vadura">
        <NestedList>
            <MudListItem T="string">@Localizer[nameof(DemizonLocales.EnglishAndGermanCommunicationText)]</MudListItem>
            <MudListItem T="string">
                <MudIcon Icon="@Icons.Material.Outlined.Email" Size="Size.Small" Class="mr-2"></MudIcon>
                janab.kolenc@gmail.com
            </MudListItem>
            <MudListItem T="string"><MudIcon Icon="@Icons.Material.Outlined.Phone" Size="Size.Small" Class="mr-2"></MudIcon> +420 776 642 622</MudListItem>
        </NestedList>
    </MudListItem>

    <MudListItem T="string" Expanded="true" Text="Jiří Rybecký">
        <NestedList>
            <MudListItem T="string"><MudIcon Icon="@Icons.Material.Outlined.Phone" Size="Size.Small" Class="mr-2"></MudIcon> 720 686 138</MudListItem>
            <MudListItem T="string"><MudIcon Icon="@Icons.Material.Outlined.Email" Size="Size.Small" Class="mr-2"></MudIcon> rjdesign@seznam.cz</MudListItem>
        </NestedList>
    </MudListItem>
</MudList>

@code {

    protected override void OnInitialized()
    {
        PageService.SetTitle(Localizer[nameof(DemizonLocales.Contact)]);
    }

}
