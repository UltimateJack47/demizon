@inherits LayoutComponentBase

<PageTitle>@PageService.GetTitle()</PageTitle>

<MudLayout>
    <MudAppBar Elevation="2" Dense="true">
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit"
                       Edge="Edge.Start" OnClick="@(_ => DrawerToggle())"
                       aria-label="Otevřít navigační menu"/>
        <MudSpacer/>
        <MudIconButton Icon="@Icons.Material.Filled.Language" Color="Color.Inherit" Size="Size.Small"
                       title="Jazyk / Language"/>
        <MudButton Size="Size.Small" Color="Color.Inherit" OnClick="@(() => SetCulture("cs"))">CS</MudButton>
        <MudText Color="Color.Inherit" Style="opacity:0.5">/</MudText>
        <MudButton Size="Size.Small" Color="Color.Inherit" OnClick="@(() => SetCulture("en"))">EN</MudButton>
        <AuthorizeView>
            <Authorized>
                <MudIconButton Icon="@Icons.Material.Filled.AdminPanelSettings" Color="Color.Inherit"
                               Href="@(NavigationManager.BaseUri + "Admin/Dances")" title="Administrace"/>
                <MudIconButton Href="@(NavigationManager.BaseUri + "Logout")" Color="Color.Inherit"
                               Icon="@Icons.Material.Filled.Logout" title="Odhlásit"/>
            </Authorized>
            <NotAuthorized>
                <MudIconButton Icon="@Icons.Material.Filled.Login" Color="Color.Inherit"
                               Href="@(NavigationManager.BaseUri + "Login")" title="Přihlásit"/>
            </NotAuthorized>
        </AuthorizeView>
    </MudAppBar>

    <MudDrawer @bind-Open="DrawerOpen" Elevation="2" Variant="DrawerVariant.Responsive" ClipMode="DrawerClipMode.Always" Class="demi-admin-drawer">
        <MudDrawerHeader Class="pa-4" Style="background: linear-gradient(135deg, var(--demi-primary) 0%, var(--demi-primary-light) 100%);">
            <MudStack Spacing="0">
                <MudText Typo="Typo.h6" Style="color: white; line-height: 1.2;">FS Demižón</MudText>
                <MudText Typo="Typo.caption" Style="color: rgba(255,255,255,0.75);">Strážnice</MudText>
            </MudStack>
        </MudDrawerHeader>
        <NavMenu></NavMenu>
        <div style="margin-top: auto;">
            <MudDivider Class="pb-1"/>
            <MudStack Class="pa-3" Spacing="0">
                <MudText Typo="Typo.caption" Style="color: var(--mud-palette-text-secondary);">
                    © @DateTime.Now.Year FS Demižón Strážnice
                </MudText>
                <MudStack Row="true" Spacing="2">
                    <MudLink Href="/Privacy-Policy" Typo="Typo.caption" Color="Color.Default"
                             Style="opacity: 0.6; font-size: 0.7rem;">
                        @(_isCzech ? "Ochrana osobních údajů" : "Privacy Policy")
                    </MudLink>
                    <MudText Typo="Typo.caption" Style="opacity: 0.4; font-size: 0.7rem;">·</MudText>
                    <MudLink Href="/Terms-of-Service" Typo="Typo.caption" Color="Color.Default"
                             Style="opacity: 0.6; font-size: 0.7rem;">
                        @(_isCzech ? "Podmínky užívání" : "Terms of Service")
                    </MudLink>
                </MudStack>
            </MudStack>
        </div>
    </MudDrawer>

    <MudMainContent>
        <MudContainer MaxWidth="MaxWidth.Large" Class="pa-4">
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>

@code {
    bool DrawerOpen = true;
    bool _isCzech => Thread.CurrentThread.CurrentUICulture.Name == "cs-CZ";

    void DrawerToggle()
    {
        DrawerOpen = !DrawerOpen;
    }

    private void SetCulture(string culture)
    {
        NavigationManager.NavigateTo(NavigationManager.BaseUri + $"SetLanguage/{culture}", true);
    }
}
